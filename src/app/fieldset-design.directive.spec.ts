{
  import { FieldsetDesignDirective } from './fieldset-design.directive';

describe('FieldsetDesignDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldsetDesignDirective();
    expect(directive).toBeTruthy();
  });
});
